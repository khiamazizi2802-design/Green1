rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /compliance_docs/{userEmail}/{allPaths=**} {
      // Allow read/write access to authenticated users
      allow read, write: if request.auth != null;
    }
  }
}
